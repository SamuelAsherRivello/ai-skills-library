interface:
  display_name: "OpenSpec Archive Change"
  short_description: "Archive a completed OpenSpec change"
  default_prompt: "Use $openspec-archive-change to archive one completed OpenSpec change."
